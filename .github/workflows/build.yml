name: Build KernelSU for Red Magic 6
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Download Source Code Directly (Skip Git)
        run: |
          cd $GITHUB_WORKSPACE
          curl -L -o kernel.zip https://github.com/ztemt/NX669J-kernel/archive/refs/heads/main.zip
          unzip -q kernel.zip
          
          if [ -d "NX669J-kernel-main" ]; then
            mv NX669J-kernel-main/* . || true
            rm -rf NX669J-kernel-main
          fi
          rm -f kernel.zip

      - name: Setup Build Environment & Install LLVM Tools
        run: |
          sudo apt update
          sudo apt install -y bc bison build-essential ccache curl flex g++-multilib gcc-multilib git gnupg gperf lib32readline-dev lib32z1-dev libelf-dev liblz4-tool libsdl1.2-dev libssl-dev libxml2 libxml2-utils lzop pngcrush rsync schedtool squashfs-tools xsltproc zip zlib1g-dev lld llvm libssl-dev pkg-config libmpc-dev libmpfr-dev libgmp-dev

      - name: Fetch Official Clang & GCC Toolchains via Git
        run: |
          # 1. تحميل الـ Clang الرسمي لـ أندرويد
          git clone --depth=1 https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86 $GITHUB_WORKSPACE/clang-prebuilt
          mkdir -p $GITHUB_WORKSPACE/clang
          if [ -d "$GITHUB_WORKSPACE/clang-prebuilt/clang-r418222b" ]; then
            mv $GITHUB_WORKSPACE/clang-prebuilt/clang-r418222b/* $GITHUB_WORKSPACE/clang/
          elif [ -d "$GITHUB_WORKSPACE/clang-prebuilt/clang-stable" ]; then
            mv $GITHUB_WORKSPACE/clang-prebuilt/clang-stable/* $GITHUB_WORKSPACE/clang/
          fi
          rm -rf $GITHUB_WORKSPACE/clang-prebuilt

          # 2. تحميل الـ GCC الاحتياطي (اللازم لبعض ملفات الـ الروابط القديمة في الـ Host)
          git clone --depth=1 https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9 $GITHUB_WORKSPACE/gcc64

      - name: Integrate KernelSU Manually
        run: |
          cd $GITHUB_WORKSPACE
          TARGET_DIR="."
          if [ -d "msm-5.4" ]; then TARGET_DIR="msm-5.4"; fi
          cd $TARGET_DIR
          
          mkdir -p drivers/kernelsu
          curl -L https://github.com/tiann/KernelSU/archive/refs/heads/main.zip -o ksu.zip
          unzip -q ksu.zip
          
          if [ -d "KernelSU-main" ]; then
            mv KernelSU-main/* drivers/kernelsu/ || true
            rm -rf KernelSU-main
          fi
          rm -f ksu.zip

          if [ -f "drivers/Makefile" ]; then
            echo "obj-y += kernelsu/" >> drivers/Makefile
          fi

      - name: Fix Source Code & Force Inject Configurations
        run: |
          cd $GITHUB_WORKSPACE
          if [ -d "msm-5.4" ]; then cd msm-5.4; fi
          
          # تنظيف ملف الـ Kconfig الخاص بالإضاءة لمنع تحذيرات الترميز
          if [ -f "drivers/leds/nubia_led/aw8695/Kconfig" ]; then
            tr -cd '\11\12\15\40-\176' < drivers/leds/nubia_led/aw8695/Kconfig > drivers/leds/nubia_led/aw8695/Kconfig.clean
            mv drivers/leds/nubia_led/aw8695/Kconfig.clean drivers/leds/nubia_led/aw8695/Kconfig
          fi

          # تعطيل بناء أدوات التوقيع لمنع تعارضات الـ OpenSSL القديمة
          if [ -f "scripts/Makefile" ]; then
            sed -i 's/hostprogs-always-y += sign-file/# hostprogs-always-y += sign-file/g' scripts/Makefile
            sed -i 's/hostprogs-always-y += extract-cert/# hostprogs-always-y += extract-cert/g' scripts/Makefile
            sed -i 's/hostprogs-$(CONFIG_SYSTEM_EXTRA_CERTIFICATE) += insert-sys-cert/# hostprogs-$(CONFIG_SYSTEM_EXTRA_CERTIFICATE) += insert-sys-cert/g' scripts/Makefile
          fi

          # تجهيز مجلد المخرجات وملف التكوين مباشرة
          mkdir -p out
          if [ -f "arch/arm64/configs/vendor/lahaina-qgki-perf_defconfig" ]; then
            cp arch/arm64/configs/vendor/lahaina-qgki-perf_defconfig out/.config
          elif [ -f "arch/arm64/configs/lahaina-perf_defconfig" ]; then
            cp arch/arm64/configs/lahaina-perf_defconfig out/.config
          elif [ -f "arch/arm64/configs/lahaina_defconfig" ]; then
            cp arch/arm64/configs/lahaina_defconfig out/.config
          fi
          
          # إخماد الحماية بالكامل وتأكيد الإعدادات
          if [ -f "out/.config" ]; then
            sed -i '/CONFIG_KSU/d' out/.config
            sed -i '/CONFIG_MODULE_SIG/d' out/.config
            sed -i '/CONFIG_SYSTEM_TRUSTED_KEYS/d' out/.config
            
            echo "CONFIG_KSU=y" >> out/.config
            echo "CONFIG_MODULE_SIG=n" >> out/.config
            echo "CONFIG_MODULE_SIG_ALL=n" >> out/.config
            echo 'CONFIG_SYSTEM_TRUSTED_KEYS=""' >> out/.config
          fi

      - name: Force Inject Linkers into Toolchains & Build
        run: |
          cd $GITHUB_WORKSPACE
          
          # الخدعة الكبرى: نسخ الـ ld.lld الفعلي من السيستم وحقنه داخل مجلدات الـ bin الخاصة بالـ Clang والـ GCC لضمان عدم اختفائه
          cp /usr/bin/ld.lld $GITHUB_WORKSPACE/clang/bin/ld.lld
          cp /usr/bin/ld.lld $GITHUB_WORKSPACE/clang/bin/aarch64-linux-gnu-ld.lld
          cp /usr/bin/ld.lld $GITHUB_WORKSPACE/gcc64/bin/aarch64-linux-android-ld.lld
          
          # ربط الـ ld العادي بالـ lld أيضاً كخطوة أمان قصوى
          ln -sf $GITHUB_WORKSPACE/clang/bin/ld.lld $GITHUB_WORKSPACE/clang/bin/ld
          ln -sf $GITHUB_WORKSPACE/clang/bin/ld.lld $GITHUB_WORKSPACE/gcc64/bin/aarch64-linux-android-ld

          if [ -d "msm-5.4" ]; then cd msm-5.4; fi
          
          # تصدير المسارات للبيئة الحالية
          export PATH=$GITHUB_WORKSPACE/clang/bin:$GITHUB_WORKSPACE/gcc64/bin:$PATH
          
          # أمر التكوين النهائي مع تمرير البيئة الكاملة
          make O=out olddefconfig ARCH=arm64 CC=clang LLVM=1 LLVM_IAS=1 CLANG_TRIPLE=aarch64-linux-gnu- CROSS_COMPILE=aarch64-linux-android-
          
          # بدء البناء الفعلي بكامل الأنوية المتوفرة
          make O=out ARCH=arm64 CC=clang LLVM=1 LLVM_IAS=1 CLANG_TRIPLE=aarch64-linux-gnu- CROSS_COMPILE=aarch64-linux-android- -j$(nproc)

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Kernel_Image_with_KernelSU
          path: "**/out/arch/arm64/boot/Image"
